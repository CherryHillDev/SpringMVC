<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userMapping">
	<insert id="insertUser">
		INSERT INTO user(username, password, authority, email) VALUES(#{username}, #{password}, "USER", #{email})
	</insert>
	
	<update id="updateUser">
		UPDATE user SET password=#{password} WHERE username=#{username}
	</update>
	
	<delete id="deleteUser">
		UPDATE user SET enabled=0 WHERE username=#{username}
	</delete>
	
	<select id="getUser" resultType="com.kj.springboard.user.UserVO">
		SELECT * FROM user WHERE username=#{username}
	</select>
	
	
	<!-- user_board -->
	<insert id="insertUserBoard">
		INSERT INTO user_board(username, board_id) VALUES(#{username}, #{board_id})
	</insert>
	
	<delete id="deleteUserBoard">
		DELETE FROM user WHERE username=#{username} and board_id=#{board_id}
	</delete>
	
	<select id="getUserBoard" resultType="com.kj.springboard.user.UserBoardVO">
		SELECT * FROM user_board WHERE username=#{username} and board_id=#{board_id}
	</select>
	
	<select id="getUserBoardCount">
		SELECT count(*) FROM user_board WHERE board_id=#{board_id}
	</select>
</mapper>