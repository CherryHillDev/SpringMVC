<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapping">
	<insert id="insertBoard">
		INSERT INTO board(title, writer, content) VALUES(#{title}, #{writer}, #{content})
	</insert>
	<update id="updateBoard">
		UPDATE board SET title=#{title}, writer=#{writer}, content=#{content} WHERE id=#{id}
	</update>
	
	<update id="updateBoardCount">
		UPDATE board SET count=count+1 WHERE id=#{id}
	</update>
	
	<update id="updateBoardLove">
		UPDATE board SET love=love+1 WHERE id=#{id}
	</update>
	
	<delete id="deleteBoard">
		DELETE FROM board WHERE id=#{id}
	</delete>
	
	<select id="getBoard" resultType="com.kj.springboard.board.BoardVO">
		SELECT * FROM board WHERE id=#{id}
	</select>
	<select id="getBoardList" resultType="com.kj.springboard.board.BoardVO">
		SELECT b.id, title, writer, content, count, created_time, (SELECT count(*) FROM comment WHERE board_id=b.id) AS comment_count, (SELECT count(*) FROM user_board WHERE board_id=b.id) AS love_count 
		FROM board b WHERE
		<if test="condition=='title'">
			title
		</if>
		<if test="condition=='content'">
			content
		</if>
		LIKE concat('%',#{keyword},'%') ORDER BY id DESC LIMIT #{startPage}, 10
	</select>
	
	<select id="getTotalCount" resultType="int">
		SELECT count(id) FROM board WHERE
		<if test="condition=='title'">
			title
		</if>
		<if test="condition=='content'">
			content
		</if> 
		LIKE concat('%',#{keyword},'%')
	</select>
	
</mapper>